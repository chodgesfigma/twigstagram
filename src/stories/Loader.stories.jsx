import React from 'react';
import Loader from '@/components/shared/Loader'; // Update the import path as per your directory structure

export default {
  title: 'Twigstagram/shared/Loader',
  component: Loader,
};

const Template = () => <Loader />;

export const Default = Template.bind({});
